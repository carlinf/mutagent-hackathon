# deps & build
node_modules/
dist/
*.tgz

# secrets & local env  (NEVER COMMIT)
.env
.env.*
!.env.example
*.local

# agent runtime output
traces/
*.ndjson
.baseline.json
.listener-state.json
.outbox.jsonl

# os
.DS_Store

# Mutagent framework — installed locally via `bunx @mutagent/helix init`.
# Kept on disk (usable), never committed to main — fresh-install is tested separately.
.claude/
.codex/
CLAUDE.md
AGENTS.md
package.json
package-lock.json
bun.lock
